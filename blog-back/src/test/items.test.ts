import Items from "./items";

describe('Items', () => {
  it('should return the first item', () => {
    expect(Items.getFirstItem()).toBe('First item')
  })
})

describe('Items2', () => {
  it('should return the first item', () => {
    expect(Items.getFirstItem()).toBe('First item')
  })
})